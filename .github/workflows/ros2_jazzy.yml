name: ROS 2 Jazzy CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  industrial_ci:
    name: Jazzy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and test
        uses: ros-industrial/industrial_ci@master
        env:
          ROS_DISTRO: jazzy
          ROS_REPO: main
          TARGET_WORKSPACE: jsk_hand/dynamixel_detachable_hand
          UPSTREAM_WORKSPACE: >-
            .github/ros2_jazzy.repos
            -dynamixel-workbench/dynamixel_workbench
            -jsk_roseus/jsk_roseus
            -jsk_roseus/roseus_tutorials
          BEFORE_INIT: >-
            export DEBIAN_FRONTEND=noninteractive &&
            ici_asroot apt-get update &&
            ici_asroot apt-get install -y --no-install-recommends software-properties-common &&
            ici_asroot add-apt-repository -y ppa:k-okada/euslisp &&
            ici_asroot apt-get update
          ADDITIONAL_DEBS: >-
            euslisp-dev
            jskeus-dev
          ROSDEP_SKIP_KEYS: >-
            euslisp-dev
            jskeus-dev
            rviz2
